const SERVICES = {
  ACCESS_TOKEN: 'accessToken',
};

const REDUCERS = {
  USER_REDUCER: 'userReducer',
  CART_REDUCER: 'cartReducer',
};

const GLOBAL_STATES = {
  USER_PROFILE: 'userProfile',
  CARTS: 'carts',
};

export { SERVICES, REDUCERS, GLOBAL_STATES };
